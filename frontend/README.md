add  `NEXT_PUBLIC_BASE_URL` in .env

```ini
NEXT_PUBLIC_BASE_URL=backend server url
```
example `http://localhost:8001`


install & run
```bash
# install package
npm install

# running
npm run dev
```